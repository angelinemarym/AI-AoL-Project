import {StyleSheet} from 'react-native';
import colors from '../../utils/colors';

export default StyleSheet.create({
  wrapper: {
    padding: 10,
    borderRadius: 25,
    alignItems: 'center',
    marginBottom: 16,
  },
});
